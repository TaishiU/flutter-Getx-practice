import 'package:get/get.dart';

class StudentController extends GetxController {
  var name = 'Tom'.obs;
  var age = 25.obs;
  // late final String name;
  // late final int age;
  //
  // StudentController({required this.name, required this.age});
}
